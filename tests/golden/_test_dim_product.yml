table_name: .kimball_golden_golden_seq3.dim_product
table_type: dimension
scd_type: 1
keys:
  surrogate_key: product_sk
  natural_keys: [product_id]
sources:
  - name: .kimball_golden_raw_golden_seq3.products
    alias: p
    cdc_strategy: cdf
    starting_version: 0
    primary_keys: [product_id]
transformation_sql: |
  SELECT
    product_id,
    name,
    category,
    unit_cost,
    updated_at,
    _change_type
  FROM p
audit_columns: true