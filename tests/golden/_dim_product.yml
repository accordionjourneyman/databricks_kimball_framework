table_name: workspace.kimball_golden_490c74b5.dim_product
table_type: dimension
scd_type: 1
keys:
  surrogate_key: product_sk
  natural_keys: [product_id]
sources:
  - name: workspace.kimball_golden_raw_490c74b5.products
    alias: p
    cdc_strategy: cdf
    starting_version: 0
    primary_keys: [product_id]
transformation_sql: |
  SELECT
    product_id,
    name,
    category,
    unit_cost,
    updated_at,
    _change_type
  FROM p
audit_columns: true
