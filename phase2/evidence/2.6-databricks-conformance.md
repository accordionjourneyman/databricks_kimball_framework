# Phase 2.6 Databricks Free Edition conformance

Status: **passed**  
Run date: 2026-07-24  
Job ID: `506550333697497`  
Run ID: `54427316072543`

## Runtime

- Databricks Free Edition, serverless Lakeflow Job.
- Native `dbt_task` running dbt Core 1.11.7 and dbt-databricks 1.12.2.
- SQL Warehouse: `437af890146c0e82` (`Serverless Starter Warehouse`).
- Unity Catalog: `workspace`.
- Isolated dbt schema: `phase2_dbt_conformance`.
- Isolated framework schema: `phase2_framework_conformance`.
- Dependent Serverless Python task ran the independent PySpark implementation
  and reconciliation.
- Both tasks used the effective `PERFORMANCE_OPTIMIZED` performance target.

The project was deployed as workspace files and the dbt task used
`source: WORKSPACE`. No Git push or locally running dbt client was required.

## Commands and result

The native dbt task ran seven ordered commands:

1. `dbt seed --full-refresh`
2. baseline `dbt run --full-refresh`
3. baseline `dbt snapshot`
4. baseline `dbt test`
5. final incremental `dbt run`
6. final `dbt snapshot`
7. final `dbt test`

All seven commands completed successfully. The final test summary was:

```text
Done. PASS=9 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=9
```

The dependent framework task emitted:

```text
PHASE2_DATABRICKS_CONFORMANCE={
  "tables": {
    "dim_customer": {"dbt_rows": 10, "framework_rows": 10,
                     "dbt_only": 0, "framework_only": 0,
                     "types_match": true, "classification": "equivalent"},
    "dim_product":  {"dbt_rows": 5, "framework_rows": 5,
                     "dbt_only": 0, "framework_only": 0,
                     "types_match": true, "classification": "equivalent"},
    "fact_sales":   {"dbt_rows": 9, "framework_rows": 9,
                     "dbt_only": 0, "framework_only": 0,
                     "types_match": true, "classification": "equivalent"}
  }
}
```

## Classification

| Concern | Result |
|---|---|
| Databricks SQL dialect | Equivalent for the included models after replacing local hard-coded schemas with portable `target.schema` expressions. |
| Unity Catalog | Supported. Both implementations created and read isolated three-level namespace relations in the `workspace` catalog. |
| Native dbt task | Supported in Free Edition. It ran on Serverless Jobs compute and submitted generated SQL to the configured warehouse. |
| Photon | No correctness difference observed. The run used the Free Edition serverless warehouse and performance-optimized job tasks; this fixture is too small for a performance conclusion. |
| CDF | Not comparable. The agreed fixture does not enable or consume Change Data Feed. |
| Serverless runtime | Supported. Environment installation, workspace-source dbt execution, snapshots, incremental merges, tests, PySpark writes, and cross-task reads all passed. |
| Artifacts | Databricks archived the dbt task logs/artifacts; task output and run metadata are also retained locally. |

Evidence files:

- `phase2/reference/evidence/databricks-run.json`
- `phase2/reference/evidence/databricks-dbt_reference-output.json`
- `phase2/reference/evidence/databricks-framework_reconciliation-output.json`

The job and the two isolated schemas are retained for inspection. They are not
deleted automatically because the Phase 2 exit criteria require preserved
evidence.

